# Тест-Драйвен Декомпозиция: Методология разработки для одиночного разработчика с ИИ-партнером

## 📖 Введение

**Проблема:** Современный разработчик-одиночка сталкивается с парадоксом — обилие инструментов и технологий не решает
проблему нехватки времени и ресурсов. ИИ-ассистенты предлагают помощь, но без структурированного подхода превращаются в
генераторы случайного кода.

**Решение:** Тест-Драйвен Декомпозиция (Test-Driven Decomposition, TDD²) — методология, которая превращает ИИ из
инструмента в архитектурного партнера через исполняемые спецификации.

## 🧠 Философская основа

### Принципы из meowNet, применяемые к разработке:

1. **Семантическая дополняемость (2.1)**
    - Человек фокусируется на "зачем" и "что"
    - ИИ фокусируется на "как" и "коде"
    - Вместе они покрывают полный цикл разработки

2. **Управляемая неопределенность (2.2)**
    - Принимаем, что ИИ не идеален
    - Создаем условия для постепенного улучшения
    - Ошибки = данные для обучения

3. **"Не оглядывайся" (2.3)**
    - Процесс важнее перфекционизма
    - Работающий код сегодня лучше идеального завтра
    - Итеративность как норма

## 📁 Структура проекта с TDD²

```
project/
├── specs/                    # СПЕЦИФИКАЦИИ (истина в последней инстанции)
│   ├── feature_a_spec.py     # Полная спецификация фичи A
│   ├── feature_b_spec.py     # Полная спецификация фичи B
│   └── validator.py          # Валидатор соответствия
├── src/                      # РЕАЛИЗАЦИЯ
│   ├── feature_a.py          # Реализация фичи A
│   └── feature_b.py          # Реализация фичи B
├── tests/                    # ТЕСТЫ (наследники спецификаций)
│   ├── test_feature_a.py     # Конкретные тесты фичи A
│   └── test_feature_b.py     # Конкретные тесты фичи B
├── ai_context/              # КОНТЕКСТ ДЛЯ ИИ
│   ├── project_context.txt   # Автосгенерированный контекст
│   └── task_templates/       # Шаблоны задач для ИИ
└── workflows/               # РАБОЧИЕ ПРОЦЕССЫ
    ├── spec_to_ai.py        # Генерация задач из спецификаций
    └── ai_to_code.py        # Преобразование ответов в код
```

## 🔄 Жизненный цикл разработки фичи

### Фаза 0: Принципиальное обоснование (5-15 минут)

Перед любой фичей — ответ на три вопроса:

1. **Зачем?** Как это соотносится с философией проекта?
2. **Что?** Какое поведение мы ожидаем?
3. **Почему сейчас?** Какой приоритет относительно других задач?

**Пример для meowNet:**

```markdown
## Контекстный сборщик: Принципиальное обоснование

**Соответствие философии:**

- Принцип минимализма (4.3) → собираем только необходимое
- Принцип локальности (4.2) → каждый узел собирает свой контекст
- Принцип симбиоза (2.4) → ИИ нужен структурированный контекст

**Проблема:** ИИ не видит полной картины проекта
**Решение:** Автоматический сбор семантического снимка
**Приоритет:** Высокий (без этого ИИ неэффективен)
```

### Фаза 1: Создание исполняемой спецификации (30-90 минут)

Спецификация — это **единый файл**, который:

1. **Определяет требования** как код
2. **Описывает интерфейсы** для реализации
3. **Содержит тесты** соответствия
4. **Генерирует задачи** для ИИ

**Структура спецификации:**

```python
"""
feature_name_spec.py
Версия: 1.0
Принцип: [ссылка на принцип проекта]
"""


# ЧАСТЬ 1: Требования как типы данных
class FeatureSpecification:
    """Спецификация требований к фиче"""

    @dataclass
    class InputSpec:
        """Что на входе"""
        required_fields: List[str]
        validation_rules: Dict[str, Callable]

    @dataclass
    class OutputSpec:
        """Что на выходе"""
        expected_format: str
        success_criteria: List[str]


# ЧАСТЬ 2: Интерфейсы для реализации  
class IFeatureImplementation:
    """Интерфейс, который должна реализовать любая реализация"""

    def process(self, input: InputSpec) -> OutputSpec:
        raise NotImplementedError


# ЧАСТЬ 3: Тесты соответствия
class TestFeatureCompliance:
    """Абстрактные тесты - проверяют ЛЮБУЮ реализацию"""

    @pytest.fixture
    def implementation(self):
        """Должен быть переопределен в тестах реализации"""
        raise NotImplementedError

    def test_requirement_1(self, implementation):
        """Тест первого требования"""
        # Проверяет, что реализация соответствует спецификации


# ЧАСТЬ 4: Генератор задач для ИИ
class AITaskGenerator:
    """Преобразует спецификацию в задачи для ИИ"""

    def generate_coding_task(self) -> str:
        """Генерирует промпт для ИИ"""
        return f"""
        Задача: Реализовать {self.__class__.__name__}
        
        Требования:
        {self._format_requirements()}
        
        Интерфейсы:
        {self._format_interfaces()}
        
        Тесты которые должны пройти:
        {self._format_tests()}
        """
```

### Фаза 2: Генерация контекста и задач для ИИ (5-10 минут)

```python
# workflow/spec_to_ai.py
def generate_ai_task(spec_path: str) -> Dict:
    """Преобразует спецификацию в задачу для ИИ"""

    # 1. Загружаем спецификацию
    spec = import_spec(spec_path)

    # 2. Собираем контекст проекта
    context = ContextBuilder().build()

    # 3. Генерируем структурированную задачу
    task = {
        "id": f"implement_{spec.__name__}",
        "spec_version": "1.0",
        "requirements": spec.extract_requirements(),
        "interfaces": spec.extract_interfaces(),
        "tests": spec.extract_test_cases(),
        "context": context[:8000],  # Ограничиваем размер
        "acceptance_criteria": [
            "✅ Все тесты из спецификации проходят",
            "✅ Реализует все методы интерфейса",
            "✅ Соответствует стилю проекта",
            "✅ Документированны публичные методы"
        ]
    }

    return task
```

### Фаза 3: Работа с ИИ (15-45 минут за итерацию)

**Промпт-шаблон для ИИ:**

```
Ты видишь спецификацию фичи и контекст проекта.

СПЕЦИФИКАЦИЯ:
{spec_summary}

КОНТЕКСТ ПРОЕКТА:
{project_context}

ТВОЯ ЗАДАЧА:
1. Реализуй класс {FeatureImplementation} с методами из интерфейса
2. Убедись что код проходит все тесты из спецификации  
3. Следуй стилю кода проекта
4. Добавь документацию в формате проекта

ВЕРНИ ТОЛЬКО:
1. Код реализации (полные файлы .py)
2. Обновленные тесты (если нужно)
3. Краткое описание изменений

НЕ ПИШИ:
- Объяснения как работает код
- Примеры использования
- Лишние комментарии
```

### Фаза 4: Верификация и интеграция (15-30 минут)

```bash
# 1. Сохраняем код от ИИ
python workflows/ai_to_code.py --ai-response response.txt --output-dir src/

# 2. Запускаем тесты
pytest tests/test_feature_impl.py -v

# 3. Если тесты не проходят - собираем контекст ошибок
ERROR_CONTEXT=$(cat test_errors.txt)
python workflows/feedback_to_ai.py --errors "$ERROR_CONTEXT" --spec spec.py

# 4. Отправляем исправленную задачу ИИ
# ... повторяем до прохождения тестов
```

### Фаза 5: Документирование и рефакторинг (15-30 минут)

```python
# После прохождения тестов
def finalize_feature(spec_path: str, impl_path: str):
    """Финализация фичи"""

    # 1. Проверяем покрытие спецификации
    coverage = check_spec_coverage(spec_path, impl_path)

    # 2. Генерируем документацию
    docs = generate_docs_from_spec(spec_path)

    # 3. Обновляем контекст проекта
    update_project_context()

    # 4. Создаем чеклист завершения
    checklist = [
        f"✅ Спецификация: {spec_path}",
        f"✅ Реализация: {impl_path}",
        f"✅ Тесты: {coverage['test_count']} пройдены",
        f"✅ Документация: {len(docs)} строк",
        f"✅ Контекст проекта обновлен"
    ]

    return checklist
```

## 🛠️ Инструментарий

### Обязательные инструменты:

1. **Сборщик контекста** (`context_builder.py`)
    - Автоматический сбор семантического снимка
    - Фильтрация шума
    - Структурированный вывод

2. **Генератор спецификаций** (`spec_template.py`)
    - Шаблоны для разных типов фич
    - Автогенерация интерфейсов
    - Валидация структуры

3. **Рабочие процессы** (`workflows/`)
    - `spec_to_ai.py` - спецификация → задача ИИ
    - `ai_to_code.py` - ответ ИИ → код
    - `feedback_loop.py` - обработка ошибок

4. **Валидатор соответствия** (`specs/validator.py`)
    - Проверка реализации против спецификации
    - Генерация отчетов
    - Отслеживание дрейфа требований

### Опциональные инструменты:

5. **Визуализатор зависимостей** (`tools/dependency_graph.py`)
    - Граф связей между спецификациями
    - Анализ воздействия изменений
    - Выявление circular dependencies

6. **Эволюционный трекер** (`tools/spec_evolution.py`)
    - История изменений спецификаций
    - Semantic versioning для требований
    - Миграционные гиды

## 📊 Метрики успеха

### Количественные:

- **Coverage Ratio**: % требований, покрытых тестами (цель: 100%)
- **AI Iterations**: среднее число итераций с ИИ на фичу (цель: < 3)
- **Spec-to-Code Time**: время от спецификации до работающего кода (цель: < 4 часов)
- **Regression Rate**: % падающих тестов после изменений (цель: < 5%)

### Качественные:

- **Ясность спецификаций**: насколько понятны требования
- **Стабильность интерфейсов**: как часто меняются публичные API
- **Автономность ИИ**: насколько ИИ справляется без помощи
- **Семантическая целостность**: соответствие философии проекта

## 🚨 Анти-паттерны и решения

### Анти-паттерн 1: "Спецификация-монстр"

**Проблема:** Одна спецификация на 1000+ строк
**Решение:** Принцип единой ответственности. Одна спецификация = одна фича.

### Анти-паттерн 2: "Тесты без спецификации"

**Проблема:** Тесты есть, но непонятно что они проверяют
**Решение:** Все тесты наследуются от `Test*Compliance` в спецификации

### Анти-паттерн 3: "ИИ-черный ящик"

**Проблема:** Непонятно как ИИ пришел к решению
**Решение:** Фиксация промптов и ответов в `ai_context/`

### Анти-паттерн 4: "Дрейф спецификации"

**Проблема:** Код ушел от изначальных требований
**Решение:** Еженедельный запуск `validator.py`

## 🔮 Расширенные сценарии

### Сценарий A: Рефакторинг существующего кода

```python
# 1. Создаем ретро-спецификацию
python
tools / create_retro_spec.py - -module
src / legacy.py - -output
specs / legacy_spec.py

# 2. На основе спецификации пишем тесты
python
tools / generate_tests_from_spec.py - -spec
specs / legacy_spec.py - -output
tests / test_legacy.py

# 3. Рефакторим с ИИ, проверяя тесты
# ... итерационный процесс
```

### Сценарий B: Миграция между версиями

```python
# specs/user_auth_spec.py
class UserAuthSpecV1:
    """Версия 1.0 (текущая)"""
    # ...


class UserAuthSpecV2:
    """Версия 2.0 (будущая)"""
    # ...


class MigrationSpec:
    """Спецификация миграции с V1 на V2"""

    @dataclass
    class BreakingChanges:
        removed_methods: List[str]
        changed_signatures: Dict[str, Tuple[str, str]]

    def generate_migration_guide(self) -> str:
        """Генерация гида по миграции"""
        # ...
```

### Сценарий C: Коллаборация с другим разработчиком

```python
# developer_branch/
# ├── specs/feature_x_spec.py   # Новая спецификация
# ├── implementation_plan.md     # План реализации
# └── ai_context/conversation.json  # Диалог с ИИ

# После code review:
# 1. Мерджим спецификацию
# 2. Запускаем тесты соответствия  
# 3. Интегрируем реализацию
```

## 💡 Ментальные модели

### Модель 1: "Спецификация как контракт"

```
Человек (клиент) → [Контракт: Спецификация] → ИИ (исполнитель)
```

### Модель 2: "Тесты как валидация контракта"

```
Реализация → [Проходит тесты?] → Да: Контракт выполнен
                              → Нет: Переговоры (итерации)
```

### Модель 3: "ИИ как детализация абстракций"

```
Абстракция (человек) → [Уточнение] → Детализация (ИИ)
     "Что" и "Зачем"      Спецификация      "Как" и "Код"
```

## 📚 Шаблоны файлов

### Шаблон спецификации (`templates/spec_template.py`):

```python
"""
{feature_name}_spec.py
Версия: 1.0
Принцип: {link_to_principle}
"""

# Импорты для типизации
from dataclasses import dataclass, field
from typing import List, Dict, Optional, Callable
from enum import Enum
import pytest


class {FeatureName}Specification:
    """Спецификация требований к {feature_name}"""

    # Часть 1: Типы данных требований
    @dataclass
    class InputSpec:
        """Спецификация входных данных"""
        # ...

    @dataclass
    class OutputSpec:
        """Спецификация выходных данных"""
        # ...

    # Часть 2: Интерфейс реализации  
    class I{FeatureName}Implementation:
        """Интерфейс для реализации"""
        # ...

    # Часть 3: Тесты соответствия
    class Test{FeatureName}Compliance:
        """Абстрактные тесты соответствия"""
        # ...

    # Часть 4: Утилиты
    @classmethod
    def generate_ai_task(cls) -> Dict:
        """Генерация задачи для ИИ"""
        # ...
```

### Шаблон тестов реализации (`templates/test_impl_template.py`):

```python
"""
test_{feature_name}_impl.py
Тестирует конкретную реализацию против спецификации
"""

import pytest
from

{feature_name}
_spec
import

(
    {FeatureName}Specification,
Test{FeatureName}Compliance
)
from src.

{feature_name}
import

{FeatureName}
Implementation


class TestOurImplementation(Test{FeatureName}Compliance):
    """Тесты нашей конкретной реализации"""

    @pytest.fixture
    def implementation(self):
        """Фикстура возвращает нашу реализацию"""
        return {FeatureName}
        Implementation()

    # Дополнительные тесты для нашей реализации
    def test_our_specific_behavior(self):
        """Тест специфичного поведения"""
        # ...
```

## 🎯 Быстрый старт

### Шаг 1: Установка

```bash
git clone https://github.com/meownet/tdd2-workflow
cd tdd2-workflow
pip install -r requirements.txt
```

### Шаг 2: Первая спецификация

```bash
python tools/new_spec.py --feature "UserAuthentication" --principle "2.4"
# Создает specs/user_authentication_spec.py
```

### Шаг 3: Работа с ИИ

```bash
# Генерируем задачу
python workflows/spec_to_ai.py --spec specs/user_authentication_spec.py --output task.md

# Отправляем ИИ (вручную или через API)
# Получаем ответ → сохраняем в response.txt

# Преобразуем в код
python workflows/ai_to_code.py --ai-response response.txt --output-dir src/
```

### Шаг 4: Тестирование

```bash
# Создаем тесты реализации
python tools/generate_impl_tests.py --spec specs/user_authentication_spec.py

# Запускаем
pytest tests/test_user_authentication_impl.py -v
```

### Шаг 5: Итерация до успеха

```bash
# Если тесты не проходят
python workflows/feedback_loop.py --test-errors test_errors.txt --spec specs/user_authentication_spec.py

# Повторяем шаги 3-4
```

## 🔄 Интеграция в существующий проект

### Для проекта meowNet:

```python
# 1. Добавляем workflow/ в .gitignore
echo
"workflows/ai_context/" >>.gitignore
echo
"workflows/temp/" >>.gitignore

# 2. Создаем начальные спецификации для core-модулей
for module in "federation" "agents" "tasks" "semantics"; do
python
tools / new_spec.py - -feature $module - -principle
"2.5"
done

# 3. Настраиваем автоматический сбор контекста
echo
"0 * * * * cd /path/to/meownet && python context_builder.py -o ai_context/project_context.txt" >> crontab

# 4. Создаем Makefile цели
cat >> Makefile << 'EOF'
tdd2 - spec:
python
tools / new_spec.py - -feature $(feature) - -principle $(principle)

tdd2 - ai:
python
workflows / spec_to_ai.py - -spec
specs /$(feature)
_spec.py

tdd2 - test:
pytest
tests / test_$(feature)
_impl.py - v
EOF
```

## 🌟 Преимущества методологии

### Для разработчика:

1. **Фокус на архитектуре**, а не синтаксисе
2. **Автоматическая документация** через спецификации
3. **Меньше контекстного переключения**
4. **Измеримый прогресс** через покрытие спецификаций

### Для ИИ:

1. **Четкий контекст** работы
2. **Ясные критерии успеха**
3. **Структурированная обратная связь**
4. **Постепенное обучение** проекту

### Для проекта:

1. **Живая документация** (спецификации всегда актуальны)
2. **Устойчивость к изменениям** (тесты как safety net)
3. **Масштабируемость** (новые разработчики понимают через спецификации)
4. **Качество кода** (проверка на соответствие стандартам)

## 🏁 Заключение

**TDD²** — это не просто "тесты перед кодом". Это философия разработки, где:

1. **Спецификация** — это единственный источник истины
2. **Тесты** — это проверка соответствия истине
3. **Код** — это реализация истины
4. **ИИ** — это инструмент воплощения истины в код

Для разработчика-одиночка это означает переход от роли **исполнителя** к роли **архитектора**. Вы больше не пишете код —
вы проектируете системы и ставите задачи. ИИ становится вашим техническим партнером, который превращает архитектурные
решения в работающий код.

Это методология для тех, кто хочет строить не просто работающие системы, а **понятные, поддерживаемые и эволюционирующие
** системы, даже в одиночку.

---

**Примечание:** Этот документ сам является спецификацией методологии TDD². Его можно использовать как шаблон для
создания собственных рабочих процессов.